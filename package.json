{
  "name": "restobill",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/check-size.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "e2e": "playwright test",
    "serve:dist": "node scripts/serve-dist.mjs",
    "seed:50k": "node scripts/gen-backup.mjs 50000 seed-50k.json"
  },
  "dependencies": {
    "idb": "^8.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "axe-core": "^4.13.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.2"
  }
}
